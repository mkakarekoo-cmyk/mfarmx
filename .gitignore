# Kopie danych użytkownika (eksporty JSON gospodarstwa) — NIE commitować
backups/
*.json
!vercel.json

# Vercel
.vercel

# System / edytory
Thumbs.db
.DS_Store
*.log

# Notatki robocze i dokumentacja wewnętrzna zostają w repo — to część projektu.
# Nic z danymi gospodarstwa nie może tu trafić: backups/ i *.json są wyżej.
